from django.db import models
from django.contrib.auth.models import AbstractUser

class User(AbstractUser):
    # Add additional fields as needed
    # username, email, and password fields are included by default
    # ...
      pass
